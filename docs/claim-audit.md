# Claim audit — Epic 25

This tracked copy is the durable Story 25.2 audit. UNVERIFIED means a named asynchronous
interleaving has no fence test; it is not a claim that the flow fails. Raw RED/GREEN receipts
remain local under `.dev-auto/evidence/epic-25/`.

**Claim audit (2026-09-25, implementation run):** every `await` boundary per flow, the fence covering it, and the disposition. All line anchors are at the implemented revision.

| Flow | Establish | Await boundaries and their fences | Release | Disposition |
| --- | --- | --- | --- | --- |
| Handoff draft paste — `sendDraftLocked` [companion-service.ts:1361](../apps/desktop/src/utility/companion-service.ts) | `claimHandoffDraft` [database-companion-store.ts:726](../apps/desktop/src/utility/database-companion-store.ts) sets draft→uncertain in one worker transaction; second claim reads `claimed:false`; edit during preview is `revisionConflict` on `expectedUpdatedAt` | pre-claim reads (getDraft :1368, sessions :1376, artifacts :1380-1384) are fenced by the claim itself (update-time conflict) and by the token-confirmed review snapshot [:661](../apps/desktop/src/utility/database-companion-store.ts); post-claim re-read of destination (:1395) and incarnation (:1401) each end in release-on-mismatch — the incarnation re-check is fenced by `companion-service.test.ts:341` (first half: read changes after claim → finished `draft`, "Destination process changed before paste"); **unverified:** no test drives destination unavailability arriving inside the post-claim re-read window (:1395-1400) — the exit is implemented but unfenced | `finishHandoffDraft` [:747](../apps/desktop/src/utility/database-companion-store.ts) accepts only the uncertain claim held; definite pre-write failures finish back to `draft` (:1397, :1403) and a completed paste finishes `accepted` (:1410), while a PTY write whose outcome is unknown deliberately leaves the claim `uncertain` (interrupted delivery is never resent) — fenced by `companion-service.test.ts:341` (second half) | UNVERIFIED: one unfenced exit, listed above — fences: `database-companion-store.test.ts:410` (one paste attempt, monotonic time), `companion-service.test.ts:205` (changed draft), `:257` (duplicate paste returns stored result), `:341` (definite pre-write failures restored, possible write uncertain); Epic 23 token receipts |
| Conversation identity — `claimConversation` [session-manager.ts:1879](../apps/desktop/src/utility/session-manager.ts) | synchronous check-and-set, no `await` between get (:1880) and set (:1883) | boundaries between claim and spawn, each fenced: the conversation-reference existence check — `session-manager.test.ts:1667` holds `conversationReferenceExists` and refuses the rival mid-window; the resuming-record store write — `:1730` holds `createResuming` and refuses the rival without stopping the spawned winner; create-vs-resume rivalry across the same windows — `:1795, 1856, 2002`; the store write in flight during a swap (:712): rollback revalidates by identity — releases the new identity only if still ours, restores the previous only if the session is still live and the identity free [:750-764](../apps/desktop/src/utility/session-manager.ts). The harness-observation path (`observeConversation`) holds its own awaits: `recordReady` [:691](../apps/desktop/src/utility/session-manager.ts) is fenced by `session-manager.test.ts:4095` (a report that beats the spawn's own record waits for it, then binds); the binding read `getConversationBinding` (:695) is revalidated by the liveness and exit-unconfirmed re-check (:699-702; the already-ended case fenced by `:3962`) — **unverified:** no fence drives a teardown landing inside that read window itself; the holder-name lookup `storedSessionName` (:708) runs on the refusal path holding no claim and writing nothing — its result only names the rival in the refusal, exercised end to end by the isolated Electron receipt (`conversationFromHook.refusalReason` quoting the holder's name); the binding write `replaceConversationBinding` (:714) runs under a swapped claim whose rollback is fenced by `:4056` | teardown and rollback identity-checked (`swapConversationClaim`, `removeIfCurrent` [:2116](../apps/desktop/src/utility/session-manager.ts)) | UNVERIFIED: one observation interleaving, listed above — fences: `session-manager.test.ts:1667, 1730, 1795, 1856, 2002` (claim-to-spawn windows and rivalry), `:3772, 3904` (rival claims), `:4056` (claim not resurrected after uncommitted swap), `:4095` (record-beating report), `:3962` (report after exit) |
| Cohort resume action — `resumeCohort` [session-manager.ts:834](../apps/desktop/src/utility/session-manager.ts) | recorded promise per idempotency key, set before any `await` (:835-838); repeats return the recording | per-row `startCohortEntry` awaits run sequentially inside the one shared action (:849+); no boundary admits a second run of the same key | none by design: entries are kept for the process lifetime — the retained record is the idempotency; growth is bounded by owner dialog actions | Protected — fences: `session-manager.test.ts:4398` (ordered rows), `:4428` (repeated click replays, starts nothing twice), stop-after-failure rows (:4458+) |
| Repeat-watch state — `observeHookEvent` [companion-service.ts:926](../apps/desktop/src/utility/companion-service.ts) | per-session notice queue (:937-938 chains onto `noticeOperations`); repeat state read and written inside the queued closure (:942-943) | `writeRepeatSegment` (:945), `listAttention` (:948), `closeAttentionByKey` (:952), `openAttention` (:957): each is revalidated — withdraw only while the row is still open (:951), open only when none is open and the live incarnation still matches (:955-956); a failed store call neither invents an effect nor loses the observation (:968-970). Arrival-order serialization behind a held `openAttention` is fenced by `companion-service.test.ts:1687`. **Unverified:** no fence drives an incarnation replacement arriving while `listAttention` is pending (the :955-956 check covers it in code), nor one arriving inside the awaited `openAttention` itself — the incarnation check :955-956 precedes that await, so a session ending inside it can still open the notice (the held-open fence `:1687` does not replace the incarnation) — nor a same-session observation queued behind a pending `writeRepeatSegment` | queue tail removes itself only when still current (:831); withdrawal closes the notice row | UNVERIFIED: three interleavings, listed above — fences: `companion-service.test.ts:1618` (opens exactly once at eight, no PTY write), `:1687` (queues behind a held open), `:1705` (owner resolution before hook withdrawal), `:1735` (store lookup failure still records the firing call) |
| Voice download slot — [voice-ipc.ts:120](../apps/desktop/src/main/voice-ipc.ts) | claim before the first `await` (:123-129), ahead of folder and installed checks | `currentFolder` and `modelInstalled` awaits revalidated by abort state and slot identity (:142); cancel during the lookup releases instead of transferring | identity-checked `release` on success, abort, failure and every pre-transfer exit (:130-132, :157-161); a failed transfer keeps its error until Dismiss | Migrated in 25.1 behind a RED-first fence (`voice-ipc.test.ts:190`, receipt `voice-fence-red.log`); release fences `:220, :259` |

Await boundaries with no covering fence are labelled **unverified** above, never protected: the
handoff paste's destination-unavailable exit during the post-claim re-read; the repeat-watch
incarnation-replacement interleavings around `listAttention` and the awaited `openAttention`, and
its segment-write-queueing window; and the conversation observation's
teardown-landing-inside-the-binding-read window. Five in total; none has a
demonstrated failing interleaving, so none is a migration under AC3; each is a fenced-test gap for a
later change to close. No other flow was touched. One semantic note, superseded by the
Sol-review repair: an undismissed failed download now owns its slot — a retry over the visible
error is refused with `{ started: false }` until Dismiss — which enforces AC3's
visible-until-Dismiss letter where the first repair had allowed the retry to claim and clear it;
the refusal is fenced RED-first (`voice-ipc.test.ts`, receipt `sol-retry-red.log`).
Process exception, recorded rather than claimed as compliance: AC3's "the fence test is committed
RED first" was executed as RED-run-before-fix with the failing run kept as a receipt, and the fence
and fix entered Git together, as in every earlier accepted epic of this project.
