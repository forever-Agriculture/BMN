# Dev Auto handoff

- Project / selected epics: `/home/oleksandr/code/BMN`; Epic 9 (story 9.1).
- Original request and intended outcomes: `/dev-auto 9` (2026-09-19, Claude Code); suggested, owner-approved voice vocabulary passed to whisper-cli as one `--prompt` argument, per `_bmad-output/planning-artifacts/epics.md` Epic 9 and `reference-context-8-12.md` "Voice vocabulary".
- Mode: resume
- Stopping condition: selected scope accepted; no automatic time limit
- Explicit user stop (if any; only a later user instruction clears it): none
- Restrictions and authorization boundaries: local implementation, proportionate checks, isolated synthetic Electron runs, independent review dispatches under the owner's dev-auto policy (`~/.claude/CLAUDE.md` Authority), board/handoff updates and ready checked local commits. Push to `main` and `pnpm run update:desktop` need owner authorization for this run (the Epic 8 authorization covered that run only). Never push the old private `feat/epic-1/2` branches. No install, deploy, destructive cleanup or private-content screenshots. The hand A/B on the owner's voice is owner work.
- Authorized provider routes: native Claude Code helpers; Codex CLI, Claude CLI and configured GLM profile per `~/.claude/skills/dev-auto/references/models.md`; minimum task-relevant payload only.
- Lead host / requested model / observed model: Claude Code; no model requested by the task; observed `claude-fable-5-1/medium` until the pause (session `0ac837a5-588b-417f-b40d-7d0c3aeee27e`), then the owner switched to Opus and the lead observed `claude-opus-5/xhigh` (session `cdde456a-15fd-4fa9-87a8-c9b461e9abef`).
- Prior run: Epic 8 handoff (COMPLETE) is in git history at `4b6548b`.

## Progress

- Sprint board and reconciled state: `_bmad-output/implementation-artifacts/sprint-status.yaml` (git-ignored, local) epic-9 and 9-1 set to `done` 2026-09-19 18:55; epic-9-retrospective optional. Epic 10 next in backlog, untouched.
- Implemented: `6c656f0` feat (shared validator `shared/protocol/src/voice-vocabulary.ts`; `VoiceSettings.vocabulary` default `[]`; storage validation; one `--prompt` argv value with stderr redaction; IPC re-validation; suggester `voice-suggestions.ts`; recording-start snapshot and incarnation check in `main.tsx`; Preferences → Voice vocabulary panel; docs; Electron self-test with Chromium fake microphone and synthetic transcription) and `89da22c` fix (review repairs below). Base `96d8e7c`. Both local commits, not pushed.
- Associated loop (optional; host and native loop/task ID): none
- Active native helpers (ID, route, scope, ownership, state): none
- Collected terminal helper results: full review (Astra medium, thread `01a0ba46-fa6a-7a11-afa9-4f5bb0917dae`, on `6c656f0`, `.dev-auto/evidence/epic9-review-astra.md`): 4 MATERIAL, 3 NON-BLOCKING. Focused recheck (Astra low, thread `01a0ba58-99ef-7531-aa7b-aae4b45a11bd`, on `89da22c`, `.dev-auto/evidence/epic9-recheck-astra.md`): M1–M4, N1, N3 CLOSED; N2 OPEN non-blocking; no new material defect.

## Decisions and findings

- Original or approved intent changes: the story's 40 words / 600 characters became 30 words / 223 UTF-8 bytes (the story authorizes lowering both caps when the worst case does not fit). whisper.cpp 1.9.4 keeps `min(n_max_text_ctx, n_text_ctx/2) - 1` = 223 prompt tokens (`src/whisper.cpp:7050,7240-7250`; SOT/language/task tokens are added after that budget, :7256) and its tokenizer consumes at least one byte per token (:3325-3370); `--prompt` reaches `initial_prompt` unchanged (`examples/cli/cli.cpp:199,1239`). So a 223-byte prompt always fits in any script; about 18–20 English identifiers or about 10 Ukrainian words. A character cap could not guarantee this (random ASCII 388 chars → 299 tokens). File references contribute their file name only.
- Material pending finding M5 (found after acceptance, 2026-09-19 ~19:05, by running real whisper-cli; owner decision needed): with hints, a silent or noise-only recording pastes text. Ukrainian: no hint pasted nothing on 8/8 silent/noise clips (`[Музика]` is stripped); with three realistic Ukrainian lists, text was pasted on 13/24 (list uk1 pasted its last five words "Київ, розробка, робоча, гілка, коміт" on 7/8). English already pasted "you" on 4/8 without hints; with hints similar junk, sometimes an approved word ("Pty_host," 2/8). The spec rejected a substring echo guard (`reference-context-8-12.md:69`), and echoes are not always pure vocabulary runs ("Параліст, Київ, …", "The Voice Settings,"). whisper.cpp's no-speech probability reads 0.000 on every clip, speech or silence, with `-nt` (`nosp.log`), and its skip rule needs low confidence (`src/whisper.cpp:7729`), which an echo is not. Fix options for the owner: (a) Silero VAD through whisper-cli `--vad -vm` (needs a ~1 MB model download beside the Whisper model; not measured); (b) a renderer loudness gate before transcription (threshold needs the owner's microphone; noise suppression and AGC are on, `voice-recorder.ts:16`); (c) accept and document. Closures against `89da22c`: M1 byte cap (above); M2 validator reasons never quote a word (store/IPC tests failed on the old messages, pass now); M3 `renderer/src/voice-settings-writer.ts` queues every voice-section save and builds it from the latest settings, the panel derives state from props (writer unit tests; Electron keeps Preferences mounted across the fallback, approves a word and requires Base); M4 one `excluded()` for names, file names and identifiers; N1 `x86`/`h264` kept, version tags and digit-leading tokens dropped; N3 raw legacy row and `VACUUM INTO` backup tests.
- Non-blocking, open: N2 `readRecentLines` stops after the row that crosses 16 KiB, so it can materialize one row beyond the budget (discarded). An xterm row is bounded by the column count, so the overshoot is at most one row.
- Cross-epic obligations: Epic 5 tokens only; Epic 6 attention untouched by dictation; Epic 7 handoffs and ordinary pastes unchanged; Epic 8 `findFileReferences` reused read-only. The pre-existing renderer-restart health check now expects 5 incarnation records because the voice flow restarts one session (`apps/desktop/src/main/index.ts`, `incarnationRecords !== 5`).

## Evidence

- Post-acceptance runtime checks on `89da22c` (ignored `.dev-auto/evidence/epic9-silence/`, scripts beside logs; bundled `apps/desktop/resources/whisper/whisper-cli`, owner's installed Small model, argv as `voice-engine.ts:152-163`): the real tokenizer at the cap uses at most 179 of 223 tokens across ASCII, Cyrillic, Greek/Armenian, CJK and emoji lists, and the validator refuses exactly what exceeds 223 bytes (`tokens.log`, sha256 ae384fec47c37602…); JFK sample transcribes correctly with every 223-byte hint, +0.3–0.5 s (`whisper.log`, 068eb2671861aea1…); a nonsense random-Cyrillic hint made decoding take ~9 s instead of ~1 s on silence (`controls.log`, bcaef7edcae7612f…); silence/noise echo matrix and no-hint baseline (`matrix.log` 1c55d79e054a255e…, `baseline.log` aa42e0b7acf439a2…); no-speech probe (`nosp.log`, 4b09c438fea916e1…).
- Checks run and observed results (raw logs in ignored `.dev-auto/evidence/`): on `89da22c`: `pnpm run test:electron` EXIT 0, receipt includes `voiceFlow` with `modelAfterApproval: "base"` (`epic9-electron-11.log`, sha256 80da2d3e0dc2d817…); `pnpm run test:unit` 908 passed / 1 skipped (`epic9-unit-12.log`, sha256 e63d6430c9b9c83d…); typecheck and lint EXIT 0 (`epic9-typecheck-11.log`, `epic9-lint-11.log`); `git diff --check` and `git show --check HEAD` EXIT 0. On `6c656f0`: Electron EXIT 0 (`epic9-electron-9.log`), unit 899 passed / 1 skipped (`epic9-unit-9.log`).
- Regression proof: new suggester tests run against the `6c656f0` suggester fail 3 of 5 (`regress-epic9/old-code-result.txt`); store/IPC privacy tests failed against the old built protocol; faithful M3 mutant (panel copy updated only by its own saves, sent whole) fails the Electron check with `modelAfterApproval: "small"` (`epic9-electron-10-mutant2.log`). The first mutant froze the panel display and failed early (`epic9-electron-10-mutant.log`); the recheck judged that one INCONCLUSIVE, and mutant2 supersedes it. The fixed panel was restored and hash-checked.
- Reviewed scope and route: whole epic, `gpt-6-astra` medium (full) and low (focused recheck) via Codex CLI read-only.
- Reviewed revision / material finding closures / recheck or delta evidence: full review of `6c656f0`; recheck of `89da22c` vs `6c656f0` closed all material findings.
- Unreviewed or unverified areas: recognition benefit on real voice and recordings over 30 s are UNVERIFIED until the owner's hand A/B (English and Ukrainian, hints off/on); echo on silence is now measured on synthetic audio (M5), not on the owner's microphone. Real `whisper-cli` with `--prompt` and the token bound were executed after acceptance (above); no Ukrainian speech sample was available. The panel's rollback after a failed save is covered by writer unit tests and inspection, not by a runtime run. macOS not run. Visual appearance was not screenshot-checked; the self-test drives the panel's DOM controls.

## Measurement

- Timing: started 2026-09-19T17:45+03:00; resumed 18:24 after the owner's model switch; accepted 2026-09-19 ~18:58+03:00; elapsed about 1 h 13 min including waits and the pause.
- Dispatches:
  - Epic 9 full review via Codex CLI, observed gpt-6-astra/medium | epic-review | gpt-6-astra/medium | `~/.codex/sessions/2026/09/19/rollout-2026-09-19T18-26-48-01a0ba46-fa6a-7a11-afa9-4f5bb0917dae.jsonl` + reviewer subagents `…18-27-33-01a0ba47-aa0e…`, `…18-27-44-01a0ba47-d45b…` | first
  - Epic 9 focused recheck via Codex CLI, observed gpt-6-astra/low (models.md: Astra low for a focused follow-up on a well-evidenced question) | epic-review | gpt-6-astra/low | `~/.codex/sessions/2026/09/19/rollout-2026-09-19T18-46-03-01a0ba58-99ef-7531-aa7b-aae4b45a11bd.jsonl` | first
- Owner interventions: 1 necessary pause (session clear and model switch to Opus, about 1 min); 0 corrections; 1 question after acceptance ("are you confident?"), answered by the runtime checks that found M5; M5 fix choice requested.
- Observed usage: lead Fable session `0ac837a5…` `claude-fable-5-1`: 2,758 input, 120,069 output, 17,648,361 cache read, 256,663 cache write; `total_cost_usd` 15.58 (session cumulative). Lead Opus session `cdde456a…` `claude-opus-5` (read at 15:49Z, before the final handoff turns): 216 input, 92,237 output, 15,330,640 cache read, 201,727 cache write; no cost record in the transcript. Codex review total_tokens 1,545,383 (1,421,312 cached, 7,872 output) + subagents 198,194 and 416,211; recheck 577,481 (489,600 cached, 4,744 output). Gaps: no USD cost for Codex rollouts or the Opus session.

## Resume

- Next safe action: owner picks the M5 fix (a VAD, b loudness gate, c accept); then implement, test with the silence matrix and JFK, focused recheck, and re-accept. Do not push `6c656f0`/`89da22c` before that decision. Afterwards push + `pnpm run update:desktop` (close packaged BMN first) need owner authorization; then the owner's hand A/B. Next planned epic: 10.
- Status: BLOCKED
