# Dev Auto handoff

- Project / selected epics: `/home/oleksandr/code/BMN`; Epic 9 (story 9.1).
- Original request and intended outcomes: `/dev-auto 9` (2026-09-19, Claude Code); suggested, owner-approved voice vocabulary passed to whisper-cli as one `--prompt` argument, per `_bmad-output/planning-artifacts/epics.md` Epic 9 and `reference-context-8-12.md` "Voice vocabulary".
- Mode: resume
- Stopping condition: selected scope accepted; no automatic time limit
- Explicit user stop (if any; only a later user instruction clears it): none
- Restrictions and authorization boundaries: local work, checks, isolated Electron runs, review dispatches (`~/.claude/CLAUDE.md` Authority), board/handoff updates and checked local commits. Push to `main` and `pnpm run update:desktop`: authorized by the owner for this run on 2026-09-19 ~19:07 ("when you're confident you can push to GH and update locally"); trying and replacing voice models also allowed (only Silero VAD added). Never push the old private `feat/epic-1/2` branches. No install, deploy, destructive cleanup or private-content screenshots. The hand A/B on the owner's voice is owner work.
- Authorized provider routes: native Claude Code helpers; Codex CLI, Claude CLI and configured GLM profile (GLM-5.3 used for M5) per `~/.claude/skills/dev-auto/references/models.md`; minimum task-relevant payload only.
- Lead host / requested model / observed model: Claude Code; no model requested by the task; observed `claude-fable-5-1/medium` until the pause (session `0ac837a5-588b-417f-b40d-7d0c3aeee27e`), then the owner switched to Opus and the lead observed `claude-opus-5/xhigh` (session `cdde456a-15fd-4fa9-87a8-c9b461e9abef`).
- Prior run: Epic 8 handoff (COMPLETE) is in git history at `4b6548b`.

## Progress

- Sprint board and reconciled state: `_bmad-output/implementation-artifacts/sprint-status.yaml` (git-ignored, local) epic-9 and 9-1 set to `done` 2026-09-19 18:55; epic-9-retrospective optional. Epic 10 next in backlog, untouched.
- Implemented: `7ebeb8d` fix M5 (speech gate before Whisper, below); `6c656f0` feat (shared validator, storage/IPC validation, one `--prompt` argv value, suggester, recording-start snapshot, Preferences panel, docs, Electron self-test) and `89da22c` fix (review repairs). Base `96d8e7c`. Handoff commits `418ee29`, `1f93c71` and this one; push to origin/main and `pnpm run update:desktop` follow this commit under the owner's authorization.
- Associated loop (optional; host and native loop/task ID): none
- Active native helpers (ID, route, scope, ownership, state): none
- Collected terminal helper results: full review (Astra medium on `6c656f0`, `.dev-auto/evidence/epic9-review-astra.md`): 4 MATERIAL, 3 NON-BLOCKING. Focused recheck (Astra low on `89da22c`, `epic9-recheck-astra.md`): M1–M4, N1, N3 CLOSED; N2 OPEN non-blocking; no new material defect. M5 speech gate: GLM-5.3 max review (`.dev-auto/evidence/glm-m5.md`): 1 MATERIAL (no Ukrainian speech evidence), 5 NON-BLOCKING; GLM-5.3 max recheck on the repairs (`glm-recheck-m5.md`): M1, N1, N2 CLOSED, N3–N5 accepted non-blocking, one new non-blocking (threshold duplicated in scripts) fixed with a drift test.

## Decisions and findings

- Original or approved intent changes: the story's 40 words / 600 characters became 30 words / 223 UTF-8 bytes (the story authorizes lowering both caps when the worst case does not fit). whisper.cpp 1.9.4 keeps `min(n_max_text_ctx, n_text_ctx/2) - 1` = 223 prompt tokens (`src/whisper.cpp:7050,7250`) and its tokenizer consumes at least one byte per token (:3325-3370), so a 223-byte prompt always fits in any script (about 18–20 English identifiers or 10 Ukrainian words). File references contribute their file name only.
- Material pending findings: none. M5 (found after acceptance by running real whisper-cli) CLOSED in `7ebeb8d`: with hints a silent recording pasted text (Ukrainian list echoed "Київ, розробка, робоча, гілка, коміт" 7/8; no-hint Ukrainian 0/8; English "you" pre-existing). The plan's rejected substring guard and whisper's no_speech_prob (0.000 everywhere with `-nt`) could not fix it. The owner delegated the choice; the lead chose a speech gate: `voice-engine.ts` runs whisper.cpp's stock `whisper-vad-speech-segments` with Silero v6.2.0 (`-vt 0.3 -vspd 0`) on the recording and skips Whisper at 0 segments ("No speech was recognized."); whisper argv unchanged. Gate semantics (N ≥ 1 iff a frame reaches the threshold) confirmed from `whisper_vad_segments_from_probs` by the reviewer. Threshold 0.3, below Silero's 0.5, because speech peaks measured 0.351–1.00 (lowest: "так" 30 dB quieter in noise) and non-speech ≤ 0.023, and a refusal loses dictation while a false pass only restores Whisper's old behavior. The model is the pinned source archive's `models/for-tests-silero-v6.2.0-ggml.bin`, byte-identical to ggml-org/whisper-vad `ggml-silero-v6.2.0.bin` (sha256 2aa269b7…, verified by download). The engine counts as built only with all three files; the build proves the detector on JFK (≥ 1) and silence (0) before installing; the packaged smoke test runs the packaged detector on silence. The build also rebuilds a cmake folder configured at the repository's old path `~/code/AI-Terminal`, which cmake refused (it would have failed `pnpm run package` on any stamp change). The gate applies to all dictation, which also removes the pre-existing English "you" on silence. Earlier closures against `89da22c` (M1 byte cap, M2 no word in reasons, M3 serialized voice-settings writer, M4 one `excluded()`, N1, N3): `.dev-auto/evidence/epic9-recheck-astra.md` and the `1f93c71` handoff.
- Non-blocking, open: M5-N3 detector failures read "Voice engine failed (exit N)" and share the transcription timeout; M5-N4 Windows `.exe` names (Linux-only app). Unrelated, pre-existing: `saved-output-store.test.ts` times out at 5 s under heavy load (2/3 isolated runs at load ~10), passes otherwise. N2 `readRecentLines` stops after the row that crosses 16 KiB, so it can materialize one row beyond the budget (discarded). An xterm row is bounded by the column count, so the overshoot is at most one row.
- Cross-epic obligations: Epic 5 tokens only; Epic 6 attention untouched by dictation; Epic 7 handoffs and ordinary pastes unchanged; Epic 8 `findFileReferences` reused read-only. The pre-existing renderer-restart health check now expects 5 incarnation records because the voice flow restarts one session (`apps/desktop/src/main/index.ts`, `incarnationRecords !== 5`).

## Evidence

- M5 fix checks on `7ebeb8d` (`.dev-auto/evidence/`): `pnpm run test:electron` EXIT 0 (`epic9-electron-13.log`, sha256 93e1a57dc4e55d64…); `pnpm run test:unit` 913 passed / 1 skipped (`epic9-unit-16.log`; includes a real-detector test and a threshold drift test); typecheck and lint EXIT 0 (`-14` logs); `git diff --check`, `git show --check` clean; `pnpm run voice:build` rebuilt from scratch, then idempotent (`epic9-silence/voice-build2.log`, `voice-build3.log`). Gate at 0.3 on 130 speech clips (JFK, 9 English words, 120 Piper-TTS Ukrainian: 8 texts × 3 voices × normal/−20 dB/−30 dB/noisy/−30 dB+noise) all detected, 10/10 silence and noise refused (`epic9-silence/vadgate-03.log`, cf5d85d3cd30d8d7…; peaks `uk/uk-vadp.log`, ce1c57d5382759e2…). End to end, committed vs changed `transcribeRecording` with real whisper-cli + Small: silent/noise clips pasting text old 20/27, new 0/27; 18/18 speech transcripts identical including "коміт" alone with hints (`epic9-silence/e2e-03.log`, c10fdaa535b14f00…). Regression proofs: that old-vs-new run; the build with a deaf detector (threshold 1.01) fails with "nothing was installed" and leaves resources unchanged (`voice-build-mutant.log`, 2ea2d0b4f389b9e3…); the drift test fails when the build script says 0.4; the first `voice:build` failed on the stale cmake cache (`voice-build.log`) and passed after the fix.
- Post-acceptance runtime checks on `89da22c` (`.dev-auto/evidence/epic9-silence/`, bundled whisper-cli, owner's Small model): the real tokenizer at the cap uses ≤ 179 of 223 tokens in every script tried (`tokens.log`, ae384fec47c37602…); JFK unaffected by 223-byte hints, +0.3–0.5 s (`whisper.log`); M5 matrix and no-hint baseline (`matrix.log`, `baseline.log`); no_speech_prob probe (`nosp.log`).
- Earlier checks and regression proofs for `6c656f0`/`89da22c` (Electron `epic9-electron-11.log`, unit 908/1 `epic9-unit-12.log`, suggester old-code run, privacy tests on the old protocol, M3 mutant `epic9-electron-10-mutant2.log`): the `1f93c71` handoff.
- Reviewed scope and route: whole epic, `gpt-6-astra` medium (full) and low (focused recheck) via Codex CLI read-only.
- Reviewed revision / material finding closures / recheck or delta evidence: full review of `6c656f0`; recheck of `89da22c` vs `6c656f0` closed all material findings.
- Unreviewed or unverified areas: recognition benefit on real voice and recordings over 30 s are UNVERIFIED until the owner's hand A/B (English and Ukrainian, hints off/on). The speech gate is measured on TTS and the JFK sample, not on a human voice through the owner's microphone and Chromium's noise suppression/AGC; detector time on 2-minute recordings is unmeasured (43 ms at 10 s). Padded single-word synthetic clips make Whisper loop for 10–15 s with or without hints (pre-existing). Real `whisper-cli` with `--prompt` and the token bound were executed after acceptance (above); no Ukrainian speech sample was available. The panel's rollback after a failed save is covered by writer unit tests and inspection, not by a runtime run. macOS not run. Visual appearance was not screenshot-checked; the self-test drives the panel's DOM controls.

## Measurement

- Timing: started 2026-09-19T17:45+03:00; resumed 18:24 after the owner's model switch; accepted 2026-09-19 ~18:58+03:00; reopened ~19:05 for M5; re-accepted ~19:45; elapsed about 2 h including waits and the pause.
- Dispatches:
  - Epic 9 full review via Codex CLI, observed gpt-6-astra/medium | epic-review | gpt-6-astra/medium | `~/.codex/sessions/2026/09/19/rollout-2026-09-19T18-26-48-01a0ba46-fa6a-7a11-afa9-4f5bb0917dae.jsonl` + reviewer subagents `…18-27-33-01a0ba47-aa0e…`, `…18-27-44-01a0ba47-d45b…` | first
  - Epic 9 focused recheck via Codex CLI, observed gpt-6-astra/low (models.md: Astra low for a focused follow-up on a well-evidenced question) | epic-review | gpt-6-astra/low | `~/.codex/sessions/2026/09/19/rollout-2026-09-19T18-46-03-01a0ba58-99ef-7531-aa7b-aae4b45a11bd.jsonl` | first
  - M5 speech-gate review via Claude CLI GLM profile, tools off, observed GLM-5.3 (receipt modelUsage) | complex | GLM-5.3/max | `.dev-auto/evidence/glm-m5.json` | first
  - M5 repair recheck via Claude CLI GLM profile, tools off, observed GLM-5.3 | complex | GLM-5.3/max | `.dev-auto/evidence/glm-recheck-m5.json` | first
- Owner interventions: 1 necessary pause (session clear and model switch to Opus, about 1 min); 0 corrections; 1 question after acceptance ("are you confident?"), answered by the runtime checks that found M5; the owner then delegated the fix and authorized push and desktop update.
- Observed usage: lead Fable session `0ac837a5…` `claude-fable-5-1`: 120,069 output, 17,648,361 cache read; `total_cost_usd` 15.58. Lead Opus session `cdde456a…` `claude-opus-5` (read at 16:39Z, before this handoff turn): 236,213 output, 35,047,804 cache read; no cost record. GLM-5.3 review 15,874 input / 8,970 output, $0.30; recheck 6,228 / 3,069, $0.11. Codex review total_tokens 1,545,383 + subagents 198,194 and 416,211; recheck 577,481. Gaps: no USD cost for Codex rollouts or the Opus session.

## Resume

- Next safe action: push main to origin and run `pnpm run update:desktop` (authorized; it waits for packaged BMN to exit, then packages, smoke-tests and installs). Then the owner's hand A/B, including a few silent recordings and short words said alone. Next planned epic: 10.
- Status: COMPLETE
